# competitiveprogramming
Competitive Programming setup for VSCodium.

The best option right now is **CMake**. Open the folder to see the corresponding README.md.

To get started open the `CMake` folder in VS Codium by browsing inside the `CMake` folder and running `codium .`
